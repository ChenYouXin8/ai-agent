spring:
  application:
    name: chen-image-search-mcp-server
  profiles:
    active: stdio
  # Pexels 图片搜索密钥（值来自环境变量 PEXELS_API_KEY，由父进程继承）
  pexels:
    api-key: ${PEXELS_API_KEY:}
server:
  port: 8127
